# Agent Readiness

## Architecture

The project uses a modular architecture with clear separation of concerns.

## Development

Set up development with `pip install -e ".[dev]"`

## Testing

Run tests using `pytest`

## Key Locations

- Source: `src/`
- Tests: `tests/`
- Configuration: `setup.py`
