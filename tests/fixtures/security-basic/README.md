# Security Basic Project

## Installation

```bash
npm install
```

## Security

This project includes basic security practices. For environment variables, copy `.env.example` to `.env` and configure your credentials.

## Authentication

The application uses JWT for authentication.
