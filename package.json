{
  "name": "agent-readiness-score",
  "version": "0.1.0",
  "description": "Production-readiness scoring framework for AI agent codebases",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@vercel/ncc": "^0.38.4",
    "jest": "^29.0.0"
  }
}
